# Specifies which exact Node.js version should be used for the project's
# runtime. pnpm will automatically install the specified version of Node.js and
# use it for running pnpm run commands or the pnpm node command.
# This may be used instead of .nvmrc and nvm.
use-node-version=18

# If you want to prevent contributors of your project from adding new
# incompatible dependencies, use node-version and engine-strict in a .npmrc file
# at the root of the project
node-version=18.14.2
engine-strict=true

# If this is enabled, local packages from the workspace are preferred over
# packages from the registry, even if there is a newer version of the package in
# the registry.
prefer-workspace-packages=true
